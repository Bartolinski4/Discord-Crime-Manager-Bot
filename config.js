module.exports = {

    // ? ////////^\\\\\\\\\\\ ? \\
        //    ?Ważne?   \\
    // ? ////////^\\\\\\\\\\\ ? \\

    // Token Bota
        token: "token", // Ustaw Token Bota

    // Nazwa Serwera
        nazwaSerwera: " ", // Wpisz nazwe swojego serwera

        serverIP: "XX.XX.XX.XX", // Podaj IP swojego serwera BEZ portu!
        serverPort: "XX", // Podaj port
        
    // ? ////////^\\\\\\\\\\\ ? \\
        //  ?Crime System? \\
    // ? ////////^\\\\\\\\\\\ ? \\

        serwer: " ", // Serwer na, którym działa cały system organizacji. (ID SERWERA)
        crime_org: " ", // Rola ktora ma kazdy (ID ROLI)
        crime_zarzad: " ", // Rola zarzadu organizacji (ID ROLI)
        crime_stworzyciel: " ",  // Rola ktora moze uzywac komend crime (ID ROLI)
        crime_kanal: " ", // Kanal gdzie mozna uzywac komend (ID KANAŁU)
        crime_logi: " ", // Kanal z logi (ID KANAŁU)
        crime_opiekun: " ", // Rola opiekuna crime (ID ROLI)
        crimeWarnKanal: " ",  // Kanał gdzie wysyłane są informacje z warnach org i zawiasach (ID KANAŁU) 
        
}